import { PageStub } from "@/components/page-stub";

export default function Page() {
  return <PageStub title="Home" description="Your project at a glance: status, next milestone, recent updates." />;
}
