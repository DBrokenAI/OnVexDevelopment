import { PageStub } from "@/components/page-stub";

export default function Page() {
  return <PageStub title="Clients" description="The companies OnVex builds for. Add members, link sites, see history." />;
}
