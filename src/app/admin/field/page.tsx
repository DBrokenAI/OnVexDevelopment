import { PageStub } from "@/components/page-stub";

export default function Page() {
  return <PageStub title="Field Mode" description="iPad-optimized quick entry: add lead, drop a client note, capture a voice memo." />;
}
