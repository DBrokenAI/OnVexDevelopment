import { PageStub } from "@/components/page-stub";

export default function Page() {
  return <PageStub title="Leads" description="CRM pipeline for prospects. Drag through stages, log calls, convert to clients." />;
}
