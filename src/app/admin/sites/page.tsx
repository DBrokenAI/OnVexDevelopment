import { PageStub } from "@/components/page-stub";

export default function Page() {
  return <PageStub title="Sites" description="Manage every site OnVex maintains — status, stack, monitor, last deploy." />;
}
