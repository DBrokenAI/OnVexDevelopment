import { PageStub } from "@/components/page-stub";

export default function Page() {
  return <PageStub title="Tasks" description="Backlog and active work across clients, with priority and due dates." />;
}
